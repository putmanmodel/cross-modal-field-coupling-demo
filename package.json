{
  "name": "cross-modal-field-coupling-demo",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "demo": "npm run build && node dist/run_demo.js",
    "demo:interactive": "npm run build && node dist/interactive_demo.js"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
